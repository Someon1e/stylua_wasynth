[package]
name = "stylua_spider"
version = "0.1.0"
edition = "2021"

[dependencies]
stylua = { version = "2.5.2", default-features = false, features = ["luau"] }

[profile.release]
lto = true
codegen-units = 1
